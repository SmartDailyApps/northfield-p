{
  "$comment": "WEB-UX1 runtime-origin allowlist (FREEZE §5). Until rollout phases land, every origin here is report-only context; after each disposition executes, the origin must be REMOVED from this file so validate-site --strict-origins fails if it reappears. formspree.io is a retained documented processor.",
  "origins": {
    "cdn.tailwindcss.com": "REPLACE via pinned local Tailwind CLI -> assets/css/main.min.css (Phase 3 pilot, Phase 4/5 rollout)",
    "fonts.googleapis.com": "SELF-HOST Inter/Outfit woff2 (Phase 3/5)",
    "fonts.gstatic.com": "SELF-HOST font binaries (Phase 3/5)",
    "unpkg.com": "PIN/SELF-HOST/REPLACE aos@2.3.4, ionicons@7.1.0, @formspree/ajax@1 (Phase 3/5)",
    "formspree.io": "RETAIN: documented form processor (feedback + subscription); disclosure stays aligned via WEB-LEG1",
    "play.google.com": "RETAIN: official Google Play badge image (img src); standard-store-badge practice; revisit self-hosting in Phase 5 only if it measurably hurts CWV"
  }
}
